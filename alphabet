#include <stdio.h>

int main(void)
{
	char ch;
	printf("input char: ");
	scanf("%c", &ch);
	
	int isAlpha;
	isAlpha = (ch >= 'A') && (ch <= 'Z') || (ch >= 'a') && (ch <= 'z'); 
	
	printf("char: %c\t isAlpha: %d\n", ch, isAlpha);
	
	return 0;
}
