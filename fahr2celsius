#include <stdio.h>

int main(void)
{
	int fahr;
	int cels;
	
	fahr = 100;
	
	//cels = 5 / 9 * (fahr - 32);
	//cels = 5.0 / 9.0 * (fahr - 32);
	//cels = 5 * (fahr - 32) / 9;
	cels = (fahr - 32) * 5 / 9;
	
	printf("fahrenheit = %d -> celsius = %d\n", fahr, cels);
	
	return 0;
}
